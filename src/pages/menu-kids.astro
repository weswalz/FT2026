---
import BaseLayout from '../layouts/BaseLayout.astro';
import Hero from '../components/Hero.astro';
import MenuSection from '../components/MenuSection.astro';
import { getMenuWithItems } from '../lib/menus.js';
import { getPageContent, pickSection, getSeoMeta } from '../lib/content.js';

type HeroSection = {
  title?: string;
  subtitle?: string;
  image?: string;
  height?: 'small' | 'medium' | 'large' | 'full';
};

const menu = getMenuWithItems('kids');
const cmsPage = getPageContent('menu-kids');
const seo = getSeoMeta(cmsPage, {
  title: 'Kids Menu | Field & Tides',
  description: 'Kid-friendly favorites.'
});

const heroSection = pickSection<HeroSection>(cmsPage, 'hero', {});
const hero = {
  title: heroSection?.title ?? 'Kids Menu',
  subtitle: heroSection?.subtitle ?? 'Kid-friendly favorites',
  image: heroSection?.image ?? '/images/hero-kids.jpg',
  height: heroSection?.height ?? 'small'
};
---

<BaseLayout
  title={seo.title}
  description={seo.description}
  keywords={seo.keywords}
  image={seo.image}
  heroFocus={true}
>
  <Hero {...hero} />

  <section class="section">
    <div class="container-custom max-w-5xl">
      {menu ? (
        Object.entries(menu.sections).map(([sectionTitle, items]) => (
          <MenuSection title={sectionTitle} items={items} columns={2} />
        ))
      ) : (
        <div class="text-center py-12">
          <p class="text-xl text-midnight/70">Menu coming soon...</p>
        </div>
      )}
    </div>
  </section>
</BaseLayout>
